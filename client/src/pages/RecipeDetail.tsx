import BottomNavBar from "@/components/ui/BottomNavBar";

const RecipeDetail = () => {
  return (
    <div>
      RecipeDetail
      <BottomNavBar />
    </div>
  );
};

export default RecipeDetail;
